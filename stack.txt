Fedora
Go - go version go1.26.2 linux/amd64
podman - 5.8.2
postgresql(psql) - psql (PostgreSQL) 18.3(sudo dnf install postgresql)


Go project

1. Initalize the go project with 
    go mod init todo-app
2. run a server/listen for connection on port using the 
    http.ListenAndServe
3. Initalize the server mux(multiplexer whihc has a pattern and handler function)
    - based on the request pattern(routes) handler function will get executed
4. using chi for server mux to declare routes
5. use middleware which wrap around the handler function
